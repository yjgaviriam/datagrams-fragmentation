import { SeparateFormatPipe } from './separate-format.pipe';

describe('SeparateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new SeparateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
