import { SystemFormatPipe } from './system-format.pipe';

describe('SystemFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new SystemFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
